---
import { targets } from '../../data/homeContent';
import SectionHeader from '../Ui/SectionHeader.astro';
---

<section class="container mx-auto px-[5vw] mt-16 md:mt-32 mb-16 md:mb-24 pt-10 md:pt-16 relative z-10">
    
    <!-- HEADER DELLA SEZIONE -->
    <SectionHeader 
        subtitle="Target" 
        title="Lavoriamo con chi progetta,<br>decide e vive i luoghi."
    >
        <!-- Testo introduttivo -->
        <p class="mt-4 md:mt-6 text-nova-text text-[0.92rem] md:text-[1.1rem] font-light leading-[1.5] md:leading-[1.7]">
            Novaterra è pensata per tecnici, privati ed enti pubblici che cercano un’impresa organizzata.
        </p>
    </SectionHeader>

    <!-- GRIGLIA DELLE CARD -->
    <div class="grid grid-cols-3 md:grid-cols-3 gap-3 md:gap-8">
        {targets.map((item) => (
            <div class="bg-nova-surface p-4 md:p-10 border border-white/10 hover:border-nova-accent md:hover:-translate-y-2 transition-all duration-300 group reveal-fx">
                
                <!-- Icona -->
                <div class="text-[1.25rem] md:text-[2rem] text-nova-accent mb-2 md:mb-6 font-thin">{item.icon}</div>
                
                <!-- Titolo (con set:html per leggere i <br>) -->
                <h3 class="target-title text-[0.95rem] md:text-[1.5rem] font-bold text-white mb-2 md:mb-4 leading-[1.15] md:leading-[1.2] tracking-tight" set:html={item.title}></h3>
                
                <!-- Descrizione (con set:html per leggere formattazione) -->
                <p class="target-desc text-[0.78rem] md:text-[1rem] text-nova-muted leading-[1.35] md:leading-[1.7] font-light" set:html={item.desc}></p>
            </div>
        ))}
    </div>
</section>

<style>
  @media (max-width: 768px) {
    /* Keep the 3 cards visible together by clamping text */
    .target-title {
      display: -webkit-box;
      -webkit-line-clamp: 2;
      -webkit-box-orient: vertical;
      overflow: hidden;
    }

    .target-desc {
      display: -webkit-box;
      -webkit-line-clamp: 3;
      -webkit-box-orient: vertical;
      overflow: hidden;
    }
  }
</style>
